http:
  routers:
    nextcloud:
      rule: "Host(`nextcloud.home`)"
      service: nextcloud-svc
      entrypoints:
        - web
  services:
    nextcloud-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.1.72:8081"